import React from "react";
import Layout from "@components/Layout";
import GeneratorPage from "../../sections/tools/GeneratorPage";

const generator = () => {
  return (
    <Layout>
      <GeneratorPage />
    </Layout>
  );
};

export default generator;
